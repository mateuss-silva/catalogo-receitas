name: food_challenge_20210610
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_database: ^9.0.13
  flutter_modular: ^4.5.1+1
  file_picker: ^4.5.1
  shimmer: ^2.0.0
  equatable: ^2.0.3
  flutter_slidable: ^1.2.0
  awesome_dialog: ^2.2.1
  cached_network_image: ^3.2.0
  intl: ^0.17.0
  flutter_mobx: ^2.0.5
  easy_mask: ^2.0.1
  mocktail: ^0.3.0
  mockito: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  firebase_core: ^1.16.0
  firebase_storage: ^10.2.15
  modular_test: ^1.0.1+2
  mobx_codegen: ^2.0.6
  build_runner: ^2.1.10

flutter:
  uses-material-design: true

  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
